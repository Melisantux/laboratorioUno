//Acá van sólo los prototitpos de las funciones y su documentacion en dyoxigen.

/** \brief Pide un numero entero y lo retorna.
 *
 * \param char[] Mensaje para el usuario.
 * \return int El numero ingresado.
 *
 */

 //Si selecciono la funcion y hago click en el boton para documentar, ya me aparece armado.
int pedirEntero(char []);
void incrementarValor(int*);
